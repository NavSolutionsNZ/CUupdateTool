"""compare -- standalone byte-level oracle for CUupdate.exe output.

Isolated from the cuupdate/ engine package (no engine imports). Validates the
tool's merge output against hand-merged gold objects.
"""
__version__ = "1.0"
